# Contributing

Fork the repository, create a focused branch, run `./scripts/validate.sh`, and submit a pull request describing the tested ACM and OpenShift versions. Keep environment-specific names and repository URLs out of committed manifests.
