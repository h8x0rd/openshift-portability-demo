apiVersion: v1
kind: Service
metadata:
  name: {{ include "portability-demo.fullname" . }}
  labels:
    {{- include "portability-demo.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "portability-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
